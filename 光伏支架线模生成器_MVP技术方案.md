# 光伏支架线模生成器 — MVP 技术方案（界面先行版）

> 整理日期：2026-08-12
> 目标：先做出"参数设计界面"（参照《门式刚架线框生成器 V2.0》界面逻辑），界面跑通后，再把几何生成、DXF 导出、荷载等后端逻辑逐步接进来。
> 配套文档：《光伏支架参数化建模程序_方案.md》（2026-08-11，含架构/参数/截面库/s2k 分析，本方案沿用其结论）

---

## 一、先回答：Python 里的"对话框/界面"叫什么

- AutoCAD 里的对话框叫 **DCL**（Dialog Control Language，对话框控制语言）。
- Python 里没有 DCL，做桌面界面用的是 **GUI 框架**，最常用三个：
  - **tkinter**：Python 自带，零安装，界面风格朴素，截图里的工具从外观判断大概率就是 tkinter/ttk 写的。**MVP 用它。**
  - **PySide6 / PyQt**：界面现代、控件更丰富（表格、选项卡更好看），但需要安装依赖，打包体积大。**界面定型后可选升级。**
  - **CustomTkinter**：tkinter 的美化版，介于两者之间，外观更现代但仍是 tkinter 语法。

**结论：MVP 用 tkinter + ttk，够用、好打包，后续要换皮肤再迁 PySide6 也不迟（参数结构不变，只换控件层）。**

---

## 二、MVP 范围（本次只做界面，后端后续接）

**本期交付：一个可运行的界面骨架（.py 文件，双击即跑）**

1. 按设计稿把全部参数控件摆出来（支架/组件/结构/基础/截面/荷载 6 组）；
2. 参数联动：改组件尺寸/排列自动算"组件总宽/总长"，改跨数×柱距自动算"支架总长"，蓝色大字显示（同参考工具）；
3. 工程保存/打开：参数一键存成 JSON 工程文件，改天接着调；
4. 四个按钮先做占位（点击提示"后端未接入"），预览区先画坐标轴占位框；
5. 参数层与界面解耦：界面收集参数到一个 `params` 字典，后端接口只认这个字典——**这就是"先界面、后调参数"的架构保证，后面改参数逻辑不动界面。**

**后续里程碑（验收顺序）：**

| 里程碑 | 内容 | 预估 |
|---|---|---|
| M1 | 界面框架（本次） | 0.5~1 天 |
| M2 | 几何引擎接入：节点/杆件生成 + 预览区画真实线框（按构件分色） | 1~2 天 |
| M3 | DXF 导出（图层=构件分组）+ 3D3S 试导入验证 | 1 天 |
| M4 | 截面库 + 荷载参数 + 组合表 | 1~2 天 |
| M5 | 打包免安装 exe + 与手工模型校核 | 0.5~1 天 |
| 合计 | MVP 完整版 | **约 4~7 个工作日** |

---

## 三、界面布局设计（参考《门式刚架线框生成器》）

窗口 1240×820，顶部支架类型栏，左侧固定参数面板（可滚动），右侧上半 3D 预览、下半荷载+按钮：

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 光伏支架线模生成器 V1.1                                                  │
│ 支架类型[单立柱 ▼]  项目名称[____________]   [打开][保存]                │
├───────────────────────────────┬──────────────────────────────────────────┤
│ ① 组件参数（两列紧凑）        │  ④ 3D 线框预览（Canvas）                │
│   组件库   [高科545W ▼]       │  ┌──────────────────────────────────┐    │
│   组件长[2278] 组件宽[1134]   │  │                                  │    │
│   组件厚[35]   单重[28.5]     │  │    立柱=黄 斜梁=蓝 檩条=绿       │    │
│   行数[2] 列数[14]            │  │    斜撑=红 支座=白               │    │
│   组件间隙[20]                │  └──────────────────────────────────┘    │
│   组件总宽/总长（蓝色自动算） │  [刷新预览] [生成 DXF] [生成 3D3S]      │
├───────────────────────────────┤  [生成 SAP2000]                         │
│ ② 支架形式（侧面示意画布）    │                                          │
│   最低点高度[1000] 倾角[20°]  │                                          │
│   前斜撑水平距[960] 后[1808]  │                                          │
│   檩条间距[1500] 端距[150]    │                                          │
│   柱距[2000] 跨数[8] 排数[1]  │                                          │
│   前/后立柱长度、斜梁长度、    │                                          │
│   檩条根数（蓝色自动算）      │                                          │
├───────────────────────────────┼──────────────────────────────────────────┤
│ ③ 构件截面表（型号+材质等级） │  ⑤ 荷载参数（按 NB/T 10115 简化）       │
│   立柱   [槽8 ▼]   [Q235B ▼] │   恒载[0.05]  基本风压[0.35] kN/m²      │
│   斜梁   [大C ▼]   [Q355B ▼] │   体型系数[1.05]  高度系数[1.10]        │
│   檩条   [41C ▼]   [Q235B ▼] │   粗糙度[B ▼]  雪载[0.20](可选)         │
│   斜撑   [C50 ▼]   [Q235B ▼] │                                        │
│   檩托/抱箍/横担 …           │                                        │
└───────────────────────────────┴──────────────────────────────────────────┘
```

**布局规则（和参考工具一致）：**

- 参数全在左侧一列，分组用带标题的框（ttk.LabelFrame），组内两列网格：标签 + 输入框；
- 右侧预览区占最大面积，Canvas 画线框，坐标系自动适配窗口缩放；
- 底部按钮大号、分色：刷新预览=绿、DXF=深蓝、3D3S=红、SAP2000=浅蓝；
- 单位统一 mm + kN，界面顶部标注，避免二次询问。

---

## 四、控件与参数清单（界面 ↔ 后端数据映射）

界面所有控件都绑定一个参数 key，后端只读这个字典——**这就是"界面先行、参数后调"的实现方式**：

```json
{
  "project":  { "name": "某光伏项目", "unit": "mm/kN" },
  "module":   { "lib": "高科545W", "L": 2134, "W": 1150, "T": 35, "weight": 28.5 },
  "layout":   { "rows": 2, "cols": 14, "tilt": 20, "gap": 20, "ground_gap": 1000 },
  "structure":{ "type": "单桩双立柱", "bay": 2000, "bays": 8, "array_rows": 1,
                "purlin_interval": 1500 },
  "foundation":{ "type": "螺旋桩", "dia": 300, "restraint": "固接",
                "above_ground": 200, "embed": 500 },
  "sections": { "column": "槽8-Q235B", "beam": "大C-Q355B", "purlin": "41C-Q235B",
                "brace": "C50-Q235B", "purlin_seat": "", "hoop": "", "cross_arm": "" },
  "loads":    { "dead": 0.05, "live": 0.20, "wind": 0.35, "snow": 0.20,
                "roughness": "B", "mu_s": 1.05, "mu_z": 1.10 }
}
```

**各控件默认值（沿用 2026-08-11 已确认参数）：**

| 组 | 控件 | 类型 | 默认/选项 |
|---|---|---|---|
| 支架 | 支架类型 | 下拉 | 单立柱 / 单桩双立柱（支墩后期） |
| 组件 | 组件库 | 下拉 | 高科545W / 正泰550W / 锦州阳光545W / 自定义 |
| 组件 | 长/宽/厚/单重 | 数字框 | 2134 / 1150 / 35 / 28.5（切库自动带出，可改） |
| 组件 | 排列 行×列 | 数字框 | 2 × 14 |
| 组件 | 安装倾角 | 数字框 | 20°（限 5~35） |
| 组件 | 组件间隙/最低点离地 | 数字框 | 20 / 1000 mm |
| 结构 | 柱距/跨数/阵列排数 | 数字框 | 2000 / 8 / 1 |
| 结构 | 檩条间距 | 数字框 | 1500 mm |
| 基础 | 基础形式/桩径/约束 | 下拉 | 螺旋桩 / 管桩；250/300/400；固接/铰接 |
| 基础 | 高出地面/埋深 | 数字框 | 200 / 500 mm |
| 截面 | 立柱/斜梁/檩条/斜撑 | 下拉×2 | 型号 + 材质（M4 接截面库，本期先留下拉） |
| 荷载 | 恒/活/风/雪 | 数字框 | 0.05 / 0.20 / 0.35 / 0.20 kN/m² |
| 荷载 | 粗糙度 | 下拉 | A / B / C / D |
| 荷载 | 体型/高度系数 | 数字框 | 1.05 / 1.10 |
| 全局 | 刷新预览 / 生成DXF / 生成3D3S / 生成SAP2000 | 按钮 | 本期占位，M2~M3 逐个接通 |

**联动规则：**

- 切组件库 → 自动填长宽厚单重；改排列或组件尺寸 → 自动重算"总宽/总长"；
- 改柱距×跨数 → 自动重算"支架总长"；
- 参数校验：倾角越界、数值为空时按钮变灰并提示；
- 打开/保存：整份 params 存 JSON，文件名即工程名。

---

## 五、技术选型

| 环节 | 方案 | 说明 |
|---|---|---|
| 界面 | tkinter + ttk | Python 自带，零依赖 |
| 参数载体 | JSON 工程文件 | 界面 ↔ 后端唯一数据契约 |
| 预览 | tkinter Canvas + 自绘等轴测投影 | 线模只有线，画线足够 |
| 几何计算 | numpy（M2 接） | 坐标/旋转/阵列 |
| DXF 导出 | ezdxf（M3 接） | 图层=构件分组，截面/荷载文字标注 |
| 3D3S 对接 | DXF 导入（M3 验证） | 见 2026-08-11 方案 §十七 |
| 打包 | PyInstaller | 出免安装 exe |

---

## 六、已确认 / 待确认

**已确认（2026-08-12）：**

1. 支架类型放**最顶部**，第一版下拉放：单立柱 / 单桩双立柱 两种（支墩后期再加）；
2. 布局按上面四区块：左侧参数 + 截面表，右上预览 + 四个按钮（刷新预览 / 生成 DXF / 生成 3D3S / 生成 SAP2000），右下荷载；
3. 截面表每行含"型号 + 材质强度等级（如 Q235 / Q355）"；
4. 荷载区按 NB/T 10115 简化：恒载 + 基本风压 + 体型系数 + 高度系数 + 粗糙度 + 雪载（可选）。

**待确认（不阻塞界面骨架）：**

1. 组件库：高科545W / 正泰550W / 锦州阳光545W + 自定义，对吗？
2. 预览配色：立柱=黄、斜梁=蓝、檩条=绿、斜撑=红、支座=白，行不行？
3. 立柱默认截面：先留下拉，默认值用样本里的"槽8"还是"大 C"？（M4 前定即可）

---

## 七、本次交付物

1. 本方案确认后 → `pv_support_gui.py` 界面骨架（可运行）；
2. `工程示例.json` 一份默认参数模板；
3. 操作说明：怎么改参数、怎么存工程、按钮当前状态。
