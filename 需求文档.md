# 光伏支架线模生成器 — 需求文档

> 文档说明：本文档与程序版本同步迭代。每次需求变更追加一个版本条目（注明日期与编号），历史记录保留供回溯；如需回退某次改动，可对照"变更回溯记录"。
> 配套文件：《光伏支架线模生成器_MVP技术方案.md》《pv_support_gui.py》《光伏支架工程示例.json》《荷载规范城市数据.json》《抗震设防数据.json》

---

## 一、项目目标（总需求）

1. 用 Python 桌面程序（tkinter）替代 Rhino/Grasshopper，快速生成光伏支架参数化线模；
2. 界面输入组件、支架形式、构件截面、荷载等参数，实时预览支架形式（2D 侧面 + 3D 线框）；
3. 后续导出 DXF / 3D3S / SAP2000 计算模型（M3 里程碑）；
4. 面向非编程人员：界面友好、参数有默认值、有实时联动与提醒。

---

## 二、版本需求记录

### V1.2.0（2026-08-12）
1. 建立 GUI 骨架：顶部支架类型 + 项目名称，左侧参数区，右上 3D 预览，右下荷载区；
2. 左侧分三块：组件参数 / 结构布置与基础 / 构件截面表（型号 + 材质强度等级）；
3. 四个主按钮：刷新预览、生成 DXF、生成 3D3S、生成 SAP2000（本期占位）；
4. 组件库（高科545W / 正泰550W / 锦州阳光545W / 自定义）切换联动尺寸与单重；
5. 组件总宽/总长、阵列功率自动计算显示。

### V1.2.1（2026-08-12）
1. 左右分栏改为可拖动分隔条；
2. ② 支架形式：示意图支持缩放，图上标注关键尺寸；
3. ④ 3D 预览操作：中键旋转、右键平移、滚轮缩放（左键保留给将来选择）；
4. ⑤ 荷载参数分组：基本荷载 / 风压系数；
5. ③ 构件截面表收紧，避免遮挡。

### V1.2.2（2026-08-12）
1. 取消左右分栏拖动，左侧固定宽度并保证所有参数完整可见；
2. ② 支架形式：增加右键平移，缩放/平移可复位；
3. 移除立柱长度/斜梁长度/支架总长等估算文字（避免误导，M2 校核后再上真实值）；
4. ⑤ 荷载参数：恒载/雪载/基本风压竖向排列；
5. 新增【城市查表】：省→市选择，自动填入 GB50009-2012 附录E 的 50 年重现期基本风压与基本雪压（34 省级、668 城市）。

### V1.2.3（2026-08-12）
1. 数值输入框统一收窄（实际值不超过 4 位数）；
2. 地面粗糙度移至基本荷载列下方；风压系数列新增【阵风系数】（默认 1.00）；
3. 城市查表省/市压缩为一行，避免右侧遮挡；
4. 左面板加宽，③ 截面表规格/型号下拉收紧。

### V1.2.4（2026-08-12）
1. 窗口图标换成支架线框样式；
2. ② 支架形式"复位"改为严格居中显示；
3. ③ 型号下拉占满所在列宽；
4. ② 参数区紧凑化（标签改短、去冗余单位）；后经反馈 V1.2.5 恢复单位显示；
5. ⑤ 荷载新增【沿海城市风压放大系数】（默认 1.10）；
6. 界面固定尺寸，不新增左右拖动；3D 模型自动放大填满画布；
7. ④ 3D 预览改为"转盘式"：绕模型中心旋转、Z 轴保持竖直、俯仰角限小范围；
8. 地面粗糙度选项改为规范表述 A类/B类/C类/D类。

### V1.2.5（2026-08-12）
1. 阵列参数改为【品数】（后更名为榀数），3D 预览按品数生成多榀；
2. 檩条根数修正：两排组件对应 4 根檩条；
3. 单立柱改为立于斜梁中心正下方（支架左右平衡）；
4. 城市选定后基本风压/雪载自动锁定为规范值；
5. ⑤ 荷载新增【抗震设防烈度】字段（联动机制先行，数据后补）。

### V1.2.6（2026-08-12）
1. 默认城市 = 北京/北京，启动即显示真实风压/雪压（0.45 / 0.40）并锁定；
2. 城市查表增加【区县】级下拉（抗震设防按县/区区分）；
3. 沿海放大改为复选框：勾选才启用系数输入，不勾选默认 1.0（用户自行判断）；
4. "品数"改为"榀数"（木字旁，一榀框架），3D 显示"X 榀"；
5. 单立柱补上后斜撑（前/后斜撑都有）；
6. 榀数×柱距 ＞ 组件阵列总长时给出红色提醒；
7. 新增【檩条外伸】（默认 150mm，压块安装用），自动显示檩条总长。

### V1.2.7（2026-08-12）
1. 窗口整体收窄（1280→1120 宽），消除右侧大块空白；
2. ⑤ 荷载参数两段式：上段基本荷载+风压系数，下段城市查表（省/市/区县/抗震设防一行）；
3. ④ 3D 预览 XYZ 坐标移至左下角独立小坐标（与支架分离、随旋转联动）；
4. ② 新增【斜撑离地】参数（默认 300mm，斜撑从立柱离地处斜向斜梁）；
5. 抗震设防数据接入：GB50011-2010 附录A 自动抽取，437 个城市按省→市→区县匹配烈度。

### V1.2.8（2026-08-12）
1. 省下拉显示规范全称（北京市 / 河北省 / 内蒙古自治区…），市区县联动不变；
2. "沿海风压放大"改名为【沿海城市风压放大系数】，复选框改为经典勾选样式（✓）；
3. ② 支架形式示意图四周留白对称，不再贴面板边框；
4. ② 新增【斜梁长度】计算显示：= 端距×2 + (檩条数-1)×檩条间距，几何按此规则生成；
5. 榀数×柱距超限提醒改为自动换行，不被截断；
6. 新增本《需求文档》，按版本迭代记录。

### V1.2.9（2026-08-12）
1. 去掉右上角"单位：mm / kN"显示（避免冗余信息）；
2. ① 蓝色计算文字改为【组件阵列宽 / 组件阵列长】（表述更准确）；
3. 角度单位改为中文"度"（倾角输入框与示意图标注统一）；
4. ③ 构件截面表：区块标题与列头（构件/规格/型号/材质等级）居中；
5. ④ 3D 预览：左下角 XYZ 坐标缩小；地面边界框改为虚线；
6. ⑤ 荷载参数：新增【场地类别】（Ⅰ类/Ⅱ类/Ⅲ类/Ⅳ类）；
7. 沿海城市风压放大系数：勾选框与数值框间距拉近。

### V1.2.10（2026-08-12）
1. 所有数值输入框右对齐；
2. ④ 3D 预览：XYZ 坐标放大到适中大小；构件颜色图例（色块）缩小；
3. ② 支架形式：倾角数值移到画布右上角固定显示，不随缩放/平移移动；
4. ⑤ 荷载参数：省/市/区县/抗震设防选择行移到基本荷载与风压系数之上；
5. 荷载名称规范化：高度系数→风压高度系数；正压/负压体型系数→正/负风压体型系数；
6. 沿海城市风压放大系数输入框常亮显示（去掉灰白禁用背景，未勾选时按 1.0 处理）；
7. ② 斜梁长度改为数据框显示具体数值（去掉"≈"）。

### V1.2.11（2026-08-12）
1. 沿海城市风压放大系数复选框背景色与面板一致（消除灰白底色块）；
2. 预览配色调整：斜撑=粉红色、支座=红色；
3. ④ 3D 预览：XYZ 坐标再放大 1.3 倍，X/Y/Z 文字与坐标线拉开距离；
4. ② 支架形式：倾角文字左移，避免右上角遮挡；
5. ② 斜梁长度数据框宽度与上方输入框一致；隐藏斜梁/檩条总长的计算公式文字；
6. "檩条外伸"标签改为"檩条外伸长度"（输入项保留在②）；
7. ③ 构件截面表标题恢复靠左对齐；
8. "风压/雪压（规范值，已锁定）"提示移到荷载区最底部显示。

### V1.2.12（2026-08-12）
1. 省/市/区县标签移到下拉框右侧（先框后标签）；
2. ④ 3D 预览默认视角调整为"斜梁左高右低、从右前上方看"（与截图一致）；
3. ② 支架形式：倾角标注再左移，避免被面板边缘遮挡；
4. 输入框完成输入后自动取消选中高亮（焦点离开即清除选择）；
5. ② 新增【檩条悬挑】计算显示：= (檩条总长 − 柱距×榀数) / 2；
6. ② 单位补齐并统一（倾角=度、前后斜撑/端距/柱距=mm、榀数=榀），
   右列单位列弹性拉伸防止裁切；斜梁长度"mm"改为正常颜色；
7. ② 红色超限提醒上移到柱距/榀数行下方，避免被底部表格遮挡；
8. "檩条外伸长度"改回"檩条外伸"（避免界面被拉宽）。

### V1.2.13（2026-08-12）
1. 新增【背板孔距】参数（默认 1400mm，组件背板开孔间距）；
2. 取消手动"檩条间距"输入，檩条位置自动推导：每行组件两道檩条
   （距组件底边 0 与 背板孔距处），行间加组件间隙；
   例：2 行组件 → 檩条间距 1400 / 898 / 1400（898 = 组件长 − 背板孔距 + 组件间隙）；
3. 斜梁长度自动计算 = 檩条总跨度 + 端距×2，取整到 10mm
   （例：150 + 1400 + 898 + 1400 + 150 = 3998 → 4000mm）；
4. 2D/3D 预览的檩条位置与斜梁长度同步按此逻辑生成。

### V1.2.14（2026-08-12）
1. "柱距"改名为"品间距"（与工程术语一致）；
2. 檩条悬挑 = (檩条总长 − 品间距×品数)/2，自动校验：
   · 悬挑 ＞ 800mm → 提醒"品间距或品数过少，请增大"；
   · 悬挑 ＜ 0 → 提醒"品间距×品数超出檩条总长，请减小"；
3. 推导值只读化（灰显不可编辑）：檩条悬挑、斜梁长度、檩条总长
   均由前面参数自动算出，不再人工输入（与风压锁定一致）；
4. 默认品数由 3 调整为 8（品间距 2000 时悬挑 795mm，落在 0~800 合理区间）。

### V1.2.15（2026-08-12）
1. "品间距"改回"柱距"（更符合工程命名），相关提醒文字同步；
2. 檩条悬挑、斜梁长度、檩条总长三个推导结果改为灰显锁定
   （与风压/雪载锁定样式完全一致，不可编辑）。

### V1.2.16（2026-08-12）
1. 新增两个输入参数：前斜撑距梁端（距斜梁下端，默认350mm）、
   后斜撑距梁端（距斜梁上端，默认350mm）；
2. 立柱高度（推导）= 最低点高度 + 斜梁半长×sin(倾角)；
3. 前斜撑长度 = √(a²+b²−2ab·cos(90°−倾角))，
   后斜撑长度 = √(a²+b²−2ab·cos(90°+倾角))，
   其中 a = 立柱高度−斜撑离地，b = 斜梁半长−距梁端（单立柱梁中点情形）；
   双立柱时前柱/后柱高度分别取低端/高端高度，b 取距梁端值；
4. 立柱高度、前斜撑长度、后斜撑长度均为灰显推导值（不可编辑）；
5. 2D/3D 示意图斜撑上端点按"距梁端"参数定位；
6. 默认参数验证：最低点1000、倾角20°、斜梁4000 → 立柱高1684、
   前斜撑1754、后斜撑2490（mm）。

### V1.2.17（2026-08-12）
1. 新增【梁中心偏移】参数（默认150mm）：组件最低点至斜梁中心线的垂直距离
   （主要由檩条截面高度决定，M4 可从檩条截面选择自动带出）；
2. 实际立柱高度 = 最低点高度 + 斜梁半长×sin(倾角) − 梁中心偏移；
   斜梁中心线整体下移偏移量，前/后斜撑长度按修正后的立柱高度计算；
3. 斜梁长度不受影响（长细比影响仅几十毫米，忽略）；
4. 修正后默认值：立柱高1534、前斜撑1689、后斜撑2374（mm）。

### V1.2.18（2026-08-12）
1. ④ 3D 预览的檩条按【实际檩条总长】绘制（= 组件阵列长 + 2×檩条外伸），
   超出最外侧品的外伸部分也画出；品与品按柱距排布；
2. 地面示意框范围与檩条总长一致；
3. 说明：3D 图中檩条外伸为实际值（(檩条总长−(品数−1)×柱距)/2），
   与"檩条悬挑"数值框公式（(檩条总长−品数×柱距)/2）口径不同，待用户确认统一。

### V1.2.19（2026-08-12）
1. 确认统一口径：檩条悬挑 = (檩条总长 − (品数−1)×柱距) / 2，与 3D 图实际外伸一致；
2. 超限提醒同步：悬挑 ＞800 或 ＜0 时提醒用户调整柱距/品数；
3. 品跨校验改为实际值 (品数−1)×柱距 与组件阵列总长比较；
4. 默认品数调整为 9（柱距2000 → 品跨16000、悬挑795，落在合理区间，无警告）。

### V1.2.20（2026-08-12）
1. 热修复启动崩溃：update_calc 补充读取倾角/最低点高度变量
   （V1.2.16 引入斜撑计算后漏读，导致启动 NameError）；
2. 已实际启动验证通过：默认推导值 梁长4000、悬挑795、檩条总长17590、
   立柱1534、前斜撑1689、后斜撑2374；超限提醒联动正常；
3. 后续每版发布前做一次"启动冒烟测试"（导入+初始化+update），避免再出同类问题。

### V1.2.21（2026-08-12，M2 几何引擎起步）
1. 新增 pv_geometry.py 几何引擎：参数 → 节点/杆件线模
   （立柱/斜梁/檩条通长/前后斜撑/支座），支持单立柱与单桩双立柱、任意榀数；
2. "生成 DXF"按钮接入真实导出：纯手写标准 DXF（AC1015、单位mm、无第三方依赖），
   图层按构件分组 L-COLUMN / L-BEAM / L-PURLIN / L-BRACE / L-SUPPORT；
3. 实测：9 榀单立柱 71 节点/40 杆件；单桩双立柱 80 节点/49 杆件；
4. 下一步 M3：3D3S 2024 试导入验证（单位/图层/坐标），再实现 .s2k 导出。

### V1.2.22（2026-08-12，DXF 修复）
1. 修复"数据不完整"打不开问题：原手写 DXF 缺少必需表格段（BLOCKS/OBJECTS/
   BLOCK_RECORD 等），改用 ezdxf 生成标准 DXF（AC1015、单位 mm）；
2. ezdxf 回读校验通过：49 实体、图层齐全、audit 0 错误；
3. 保留完整手写 DXF 作为无 ezdxf 时的回退路径；
4. 参数均可由用户调整（以程序默认值为准，用户按工程改）；
5. 截面库计划：先做常用常规型号（C/Z/U/槽钢/角钢/方管/圆管），
   自定义截面功能放 M4；3D3S 文件路径由用户提供后解析。

### V1.2.23（2026-08-12，3D3S Excel 导入打通）
1. 依据 3D3S"表格化模型导入"模板生成 .xlsx：
   Sheet 节点信息（编号/X/Y/Z）、单元信息（单元号/节点1/节点2/材料/截面类型/截面名称）、
   单元信息（含坐标）、节点荷载（暂空），单位 mm/kN；
2. 材料映射 Q235B→Q235、Q355B→Q355；规格→3D3S截面类型映射表
   （角钢→普通角钢(等肢)、槽钢→普通槽钢、C型钢→冷弯薄壁C型钢 等，待3D3S实测校准）；
3. "生成 3D3S"按钮已接入 Excel 导出（依赖 openpyxl，已安装）；
4. 已实测：71 节点/40 杆件，截面与材料随界面选择写入。

### 里程碑确认（2026-08-12）
1. **3D3S Excel 导入实测通过**：坐标一致、导入速度快、模型线框正确显示，无报错；
2. 截面类型映射表（普通槽钢/冷弯薄壁C型钢/普通角钢(等肢)）经 3D3S 导入验证可用；
3. 发现 3D3S 自带"模块-光伏支架"选项卡（杆件库/导入导出/定义约束），
   后续截面与约束映射可参考其内置杆件库；
4. 下一步：常用截面库（A/I/W 力学属性）→ SAP2000 .s2k 导出。

### V1.2.24（2026-08-12，常用截面库 + 线模带截面）
1. 新增 pv_sections.py 常用截面库：C型钢/Z型钢/U型钢/槽钢/角钢/方钢管/
   矩形钢管/圆钢圆管/自定义 共 60+ 常用型号，含 A/I1/I2/W1/W2/单位重；
   材料：Q235B/Q355B/Q460B（钢）+ 6061-T6/6063-T5/6005-T5（铝，强度按常见牌号，
   正式项目按质保书/规范校准）；
2. 槽钢按国标 GB/T 707 表值（槽8~槽20a），其余按薄壁矩形分解计算，
   属性来源在代码内注明，供工程验收；
3. DXF 线模新增【截面信息表】文字：构件/规格/型号/材质 + A/I，CAD 打开即可核对；
4. 界面③截面下拉型号改为读取截面库（型号与库一致，选库外型号自动回退首个）；
5. 验收方式：用户在 CAD/3D3S 中查看线模截面信息，从工程角度判断是否合理；
6. 待办：支座/端部约束暂不自动生成（3D3S 内人工定义更快）；
   荷载传递计划走 SAP2000 .s2k（可带工况/荷载/组合）。

### V1.2.24 补充（2026-08-12，DXF 截面表可见性 + 3D3S 截面校准）
1. 重新生成测试 DXF：写入正确图形范围（含左下角截面信息表），
   CAD 打开后"缩放范围"即可看到截面表（构件/规格/型号/材质 + A/I）；
2. 修复界面隐患：截面库外的型号（如 41系列C型钢）不再被静默替换，
   原样保留（仅不显示 A/I）；
3. 3D3S 截面显示问题定位：多数构件显示为线、檩条截面异常大，
   疑似 3D3S 截面库无法识别我们写入的"截面类型/名称"
   （如"冷弯薄壁C型钢""41系列C型钢(41×41)"）；
   待用户提供 3D3S"杆件库"的真实截面类型名称清单后修正映射表；
4. 提示：3D3S 视图需切换"实体/截面"显示模式才可见截面轮廓。

### V1.2.25（2026-08-12，3D3S 截面名称校准）
1. 依据 3D3S 杆件库"薄壁截面"页实测名称修正映射：
   冷弯C型钢 / 卷边Z形钢 / U型卷边槽钢 / 方形空心型钢 / 矩形空心型钢；
2. 截面名称分隔符"×"改为 3D3S 格式（C 类用大写 X，角钢用小写 x，如 L50x5）；
3. 槽钢（普通槽钢）、圆管（焊接圆管）为热轧页推测名，待 3D3S 实测确认；
4. 41系列C型钢为光伏铝导轨，3D3S 库内无此名，待用户提供截面尺寸后
   加入库或改用库内 C 型钢型号测试。

### V1.2.26（2026-08-12，3D3S 截面库精确数据接入）
1. 新增 pv_3d3s_sections.py：3D3S"冷弯卷边槽钢"杆件库导出数据
   （30 个型号，A/I1/I2/Iw/It/e0 精确值，来源：3D3S 杆件库"保存至文件"）；
2. C 型钢型号与属性改用 3D3S 数据，C140X50X20X2.5 校验与 3D3S 完全一致
   （A=648、I1=1867800、I2=221100）；
3. 修正映射：C 型钢在 3D3S 中对应类型名=【冷弯卷边槽钢】；
4. 默认斜梁/檩条改为 3D3S 库内型号（斜梁 C140X50X20X2.5-Q355、
   檩条 C100X50X15X2.5-Q235），保证导入即可识别；
5. 待办：请用户用同样方式导出其他截面库（卷边Z形钢/方形空心型钢/
   矩形空心型钢/热轧槽钢/角钢/圆管）的 .dat，逐类接入精确属性。

### 截面方位角问题记录（2026-08-12）
1. 现象：3D3S 导入后 斜梁/檩条/斜撑 截面方位角不对
   （斜梁 web 水平应为竖直抗弯、檩条应垂直于斜梁上表面、斜撑应在侧面平面内竖直）；
   立柱槽8 截面未被识别（普通槽钢+槽8 名称不匹配 3D3S 热轧库）。
2. 结论：Excel 导入模板无方位角列；3D3S 文本格式 *ELE_LINE 含"绕1轴转角"字段，
   自动控制方位角需走 3D3S 文本文件导入路线。
3. 待办（需要用户提供）：
   a) 热轧页"槽钢"杆件库用"保存至文件"导出 .dat（修正立柱截面名）；
   b) 一份 3D3S 文本模型样例（或确认 3D3S 能否保存/导出文本模型），
      用于校准 *SECTION/*MATERIAL/*ELE_LINE 字段后实现带方位角的文本导出；
   c) 若无文本样例：先用 Excel 导入 + 3D3S 内人工设置方位角，
      文本路线等有样本再自动化。
4. 方位角默认期望（记入后续文本导出）：斜梁 web 竖直、檩条垂直于斜梁上表面、
   斜撑在支架侧面平面内竖直、立柱 web 在侧面平面内。

### V1.2.28（2026-08-12，3D3S 文本导出 + 方位角实测校准）
1. 新增 pv_3d3s_text.py：按 123.3D3S 真实样例输出 3D3S 文本模型
   （VERSION 2024.0 / UNIT N,MM,J,C / MATERIAL / SECTION / NODE /
    ELE_LINE 含绕1轴转角 / CONSTRAINT 柱脚固接 / STLDCASE 恒风雪）；
2. "生成 3D3S"支持 .3D3S 文本（快，带方位角）与 .xlsx 两种导出；
3. 方位角按 123.3D3S 工作模型实测：斜梁 0°、檩条 0°、立柱 90°、斜撑 90°，
   K 节点取模型外上方一点（同样例做法）；
4. Q355B 材料钢号改 Q355（消除"Q345材料"警告）；
5. 槽8 非 3D3S 库内型号 → "参数错误"警告（0 错误，不阻塞导入），
   待用户提供普通槽钢库正确型号名后修正立柱截面。

### V1.2.29（2026-08-12，槽钢名修正 + 方位角实测微调）
1. 依据新导出的普通槽钢.dat：3D3S 槽钢用方括号表示法（槽8→[8、槽10→[10），
   [8 的 A=1024 与国标槽8一致；已修正文本与 Excel 两种导出的槽钢名，
   "参数错误"警告消除；
2. 方位角按 3D3S 实测微调：立柱 90°→180°、檩条 0°→110°；
   斜梁 0°、斜撑 90° 保持正确；
3. 3D3S 文本导出主路径确认（导入快、带方位角）；Excel 保留备用。

### V1.2.30（2026-08-12，消除导入警告）
1. 槽钢截面行改为 DB 行（按库名 [8 引用，与角钢做法一致）；
2. 删除截面行后多余的"1,1,1,1,1,1,0"第二行（123.3D3S 样例中本无此行），
   "第14行参数错误"警告消除；
3. 方位角保持：斜梁 0°、檩条 110°、立柱 180°、斜撑 90°（待用户最终确认）。

### V1.2.31（2026-08-12，檩条方位角定版）
1. 檩条绕1轴转角 110°→160°（180°−倾角20°），4 根檩条统一；
2. 导入已无报错；立柱/斜梁/斜撑方位角确认正确；
3. 若 4 根檩条仍不完全平行（K 节点投影导致），后续可改为
   按檩条位置逐根设置 K 节点，保证完全一致。

### V1.2.32（2026-08-12，檩条 K 节点逐根设置）
1. 每根檩条按自身位置设置 K 节点（x 同檩条、z 上方 1000mm），
   局部 2 轴统一指向竖直方向，4 根檩条严格平行；
2. 角度统一 160°，其余构件沿用全局 K（斜梁 0°/立柱 180°/斜撑 90° 已正确）。

### V1.2.33（2026-08-12，檩条角度定版 70°）
1. 檩条绕1轴转角 160°→70°（160−90），局部轴一致；
2. 4 根檩条 K 节点仍按自身位置设置，严格平行；
3. 至此 3D3S 导入全链路验证通过：无报错、截面全识别、
   方位角（斜梁 0°/檩条 70°/立柱 180°/斜撑 90°）正确。

### V1.2.34（2026-08-12，SAP2000 .s2k 导出）
1. 新增 pv_s2k.py：按 SAP2000 V26 样本格式生成 .s2k（N,mm,C）——
   材料 Q235/Q355、截面属性自包含（A/I33/I22/W/J）、节点/杆件、
   支座全固接、局部轴转角（同 3D3S 定版值）、荷载工况 DEAD/snow/wp/wz、
   檩条分布荷载（恒=组件+附件、雪=重力、风=局部2向）、
   光伏规范组合（1.3D+1.5Wz+1.5*0.7Sn 等）、构件分组；
2. 截面库补入抗扭常数 J；
3. "生成 SAP2000"按钮已接入；
4. 待用户 SAP2000 实测：截面属性读取、风/雪荷载大小与方向、组合合理性。

### V1.2.35（2026-08-12，SAP2000 导入修正）
1. 截面行补 tf 列（Channel/Angle 翼缘厚）；C 型钢改用 Shape="Cold Formed C"
   + LipDepth（斜梁/檩条截面夸张、立柱/斜撑显示成线的根因）；
2. 荷载工况格式修正：Type=LinStatic / InitialCond=Zero /
   LoadType="Load pattern"（修复"荷载工况定义 0/4 导入失败"）；
3. 檩条局部轴转角在 SAP2000 单独设为 160°（3D3S 70°+90°），
   斜梁 0°/立柱 180°/斜撑 90° 与 3D3S 一致；
4. 待用户 SAP2000 实测确认。

### V1.2.36~37（2026-08-12，SAP2000 收尾）
1. 删除 PROGRAM CONTROL 中 SteelCode（"Chinese 2018" 不被本机 SAP2000 识别）
   → 导入 0 错误；
2. 42 警告中 40 条为 AutoSelect=No 不被识别，改为样本写法 AutoSelect=N.A.、
   SectionType=形状名、MatProp=Default；
3. 风压方向按工程要求：wz 正风压=垂直于斜梁面朝下（压向板面）、
   wp 风吸力=垂直于斜梁面朝上（吸离板面），方向相反；
4. 说明：SAP2000 单方向分布荷载仅支持坐标轴方向，故对角风载用
   Global X + Global Z 双分量表达，合力即垂直于斜梁面（与 3D3S 单箭头等价）。

### V1.2.38（2026-08-12，SAP2000 双警告消除 + 中国规范复核）
1. 材料表（MATERIAL PROPERTIES 03A - STEEL DATA）补 CoupModType="Von Mises"，
   消除导入日志最后 2 条警告（字段为空→默认值），0 错误 0 警告；
2. 复核本机 SAP2000 26/27 中国规范库：Chinese.pro / ChineseGB08.pro 均存在；
   SAP2000.dll 内置钢设计规范完整列表含 "Chinese 2018"（GB50017-2017），
   SAP2000LanguageResource.xml 含 GB50017-2017 条文翻译——规范库非缺失、未改名；
3. 在界面查看"钢框架设计→查看/显示首选规范"无 Chinese 2018，属查看入口问题：
   应使用"设计→钢框架设计→选择设计代码"（Select Design Code）选择 Chinese 2018；
4. PROGRAM CONTROL 重新写入 SteelCode="Chinese 2018"（与用户参考文件
   单桩双立柱模型-2026_中国规范-GB50017-2017.s2k 一致），待导入实测确认。

### V1.2.39（2026-08-12，界面收紧 + 警告弹窗，截图批注修改）
1. ② 支架形式参数表去掉两处常驻空行（品跨/悬挑红色警告标签平时空占两行）；
   参数有工程逻辑问题时改为在"刷新预览 / 生成 DXF / 3D3S / SAP2000"
   按钮动作时合并弹窗提示（不阻断操作）；
2. ⑤ 荷载参数"省/市/区县/抗震设防"一行整体收紧：
   省/市/区县/抗震下拉框宽度分别 5/9/10/9 → 6/7/8/8，
   标签间距 8px → 3px、下拉框间距同步压缩，消除"区县→抗震设防"大空档
   （截图批注"此处空档改小"）；
3. 版本号 V1.2.37 → V1.2.39，状态栏同步。

### V1.2.40（2026-08-12，截图批注：省市间距 + 参数行顺序）
1. ⑤ 荷载参数"省/市/区县/抗震设防"整行放入独立 Frame 横向排列，
   不再与下方恒载/雪载输入框共用 grid 列宽——"省"与"市"之间空隙
   由 70px 减到 22px（仅剩"省"标签本身），消除"省和市之间还是有
   很大空白"的批注问题；
2. ② 支架形式参数顺序调整：檩条悬挑 → 檩条总长 → 立柱高度 →
   斜梁长度 → 前斜撑长/后斜撑长（"檩条总长""立柱高度"上移至
   "檩条悬挑"下方，按批注"檩条总长和立柱高度移到这里"）；
3. 版本号 V1.2.39 → V1.2.40，状态栏同步。

### V1.2.41（2026-08-12，SAP2000 报错修复 + 3D3S 截面库并入）
1. 修复 SAP2000 导入报错：PROGRAM CONTROL 中 SteelCode="Chinese 2018"
   在本机 SAP2000 26.3.0 不被识别（报错 "Steel frame design code
   Chinese 2018 is not recognized"），已移除该字段 → 恢复 0 错误 0 警告；
   说明：参考 s2k 来自其他机器（版本/规范包不同），本机界面仍可
   在"设计→钢框架设计→选择设计代码"中手动选择规范；
2. 并入用户提供的 16 个 3D3S 截面库 .dat（381 个真实型号，含圆管 355、
   矩形管 9、槽钢 2、角钢 1 等），新增 pv_3d3s_lib.py 数据模块；
3. 截面下拉与 3D3S 导出统一按 3D3S 库真实命名：
   槽钢 [8/[10、角钢 L50x5、方管方30x2.0、矩形管矩200x100x4x6、
   U型 u41.3x41.3x2.0、Z型 Z100x40x20x2.0、圆管 φ32x2.5 等；
4. 3D3S 截面名转换修正：矩形管/方管以"矩/方"开头的 GUI 旧名
   （如矩形管100×50×3）不再被短路误判，正确转 矩100x50x3/方60x3；
   圆管旧名补 φ 前缀（圆管48×3 → φ48x3）；
5. 版本号 V1.2.40 → V1.2.41，状态栏同步。

### V1.2.42（2026-08-13，截图批注：参数行填满 + 整体边距收紧）
1. ② 支架形式只读计算字段（檩条悬挑/檩条总长/立柱高度/斜梁长度）
   由每行仅占左列、右侧留空，改为两行两列填满右侧空白：
   檩条悬挑 | 檩条总长（一行）、立柱高度 | 斜梁长度（下一行）、
   前斜撑长 | 后斜撑长（再下一行）；
2. 整体边距收紧：主框架左右 padding 10→6/4、左右分栏间距 10→6、
   ①/②/④/⑤ 面板 padding 与纵向间距压缩约 40%；
3. 3D 预览缩放保持原样（用户可手动缩放，未改动该逻辑）；
4. 版本号 V1.2.41 → V1.2.42，状态栏同步。

### V1.2.43（2026-08-13，窗口整体收窄，右边界收到绿线）
1. 核心需求：整个窗口右侧太宽、有大段空白条；把窗口右边界
   （红线）整体左移到右侧内容结束位置（绿线），参数不被遮挡；
2. 实施：root.geometry 窗口宽度 1120 → 860；
   实测窗口 860 时荷载参数区最右控件右缘余量 22px，
   参数完整可见；840 时余量 2px，820 起开始遮挡参数；
3. 3D 预览缩放、参数计算、事件绑定、导出逻辑均未改动；
4. 版本号 V1.2.42 → V1.2.43，状态栏同步。

### V1.2.44（2026-08-13，窗口 960：左侧压缩 50px + 右侧加宽）
1. 窗口宽度 860 → 960（root.geometry），右侧荷载参数
   不再遮挡数字（实测余量 108px）；
2. 左侧栏固定宽度 342px（原 392，压缩 50px）：
   left.configure(width=342) + pack_propagate(False)，
   并收紧 ①组件参数 / ②支架形式 / ③构件截面表 内部
   padding 与 padx（6→4 / 4→2 / 8→4 等），实测左侧控件零溢出；
3. 右侧区域随窗口加宽至 602px（原 452），3D 预览与
   荷载参数更宽松；3D 缩放、参数计算、事件绑定、导出逻辑未改动；
4. 版本号 V1.2.43 → V1.2.44，状态栏同步。

### V1.2.45（2026-08-13，左 380 / 右 900 定宽）
1. 窗口总宽 1296（= 左侧 380 + 间距 + 右侧 900）；
2. 左侧栏固定 380px（①组件参数 / ②支架形式 / ③构件截面表），
   实测内部控件零溢出；
3. 右侧面板固定 900px（④3D 预览 / ⑤荷载参数），更宽松；
4. 版本号 V1.2.44 → V1.2.45，状态栏同步。

### V1.2.46（2026-08-13，右 650 定宽）
1. 窗口总宽 1046（= 左侧 380 + 间距 + 右侧 650）；
2. 左侧栏固定 380px（①组件参数 / ②支架形式 / ③构件截面表），
   内部控件零溢出；
3. 右侧面板固定 650px（④3D 预览 / ⑤荷载参数），
   荷载参数余量 156px；
4. 版本号 V1.2.45 → V1.2.46，状态栏同步。

## 六、3D3S 方位角校准经验总结（2026-08-12）

**定版关系（倾角 20° 下）：**
1. 斜梁：截面 web 竖直（2 轴在竖向平面内），绕1轴转角 = 0°，全局 K 节点即可；
2. 檩条：垂直于斜梁上表面。局部轴与倾角的关系：
   先绕梁面法向 90°，再叠斜梁倾角 → 转角 = 180°−20°−90° = **70°**；
   且 4 根檩条必须**按自身位置设置 K 节点**（x 同檩条、z 上方 1000），
   局部 2 轴才统一竖直、严格平行；
3. 立柱：转角 180°（槽钢开口朝向正确），2 轴在支架侧面平面内；
4. 斜撑：90°。

**关键教训：**
1. 方位角不能猜：先从用户工作模型（123.3D3S）提取实测转角，
   再按用户逐轮实测反馈迭代（本轮 90→180、0→110→160→70，三轮收敛）；
2. 同类构件位置不同时，共用全局 K 节点会导致局部 2 轴投影不一致，
   必须按构件自身位置设 K 节点；
3. 文本格式字段严格对照真实样例：多余的第二行会导致"参数错误"；
4. 截面类型名/型号名用 3D3S 库真实名称（冷弯卷边槽钢、[8、L50x5、φ32x2.5），
   否则识别失败或生成"型号异常"；
5. 材料钢号用现行名（Q355，非 Q345）避免警告；
6. 发布前做启动冒烟测试 + 导入实测闭环，减少返工。

---

## 三、变更回溯记录（反复修改的过程）

| 项目 | 变更过程 |
|---|---|
| 左右分栏 | V1.2.1 可拖动 → V1.2.2 取消（反馈拖动不便，改固定并保证可见） |
| 支架缩放按钮 | V1.2.1 加按钮 → V1.2.4 移除（标题已说明滚轮/右键操作） |
| 参数单位 | V1.2.4 去掉 mm 单位 → V1.2.5 恢复（用户要求显示单位） |
| 阵列数量命名 | 排数 → 品数 → 榀数（木字旁，一榀框架） |
| 檩条根数 | 均布 3 根（错误）→ 修正为 4 根（两排组件每排两端各一根） |
| 沿海放大 | V1.2.4 固定输入 1.1 → V1.2.6 改复选框（用户自行判断是否沿海） |
| 单立柱位置 | 斜梁低端 → V1.2.5 斜梁中心正下方（左右平衡） |
| 单立柱斜撑 | 仅前斜撑 → V1.2.6 补后斜撑 |
| 支架图复位 | V1.2.4 修正为严格居中 |
| 3D 预览旋转 | 自由旋转（Z 轴易倾斜）→ V1.2.4 转盘式（Z 轴保持竖直） |
| 斜梁长度规则 | 按组件总宽 → V1.2.8 按端距×2 + (檩条数-1)×间距 计算并显示 |

---

## 四、当前状态与后续里程碑

- 当前：界面骨架 V1.2.8，参数输入、城市/抗震查表、2D/3D 预览、工程保存打开均已可用；
- M2：几何引擎（按榀数生成节点/杆件真实线模，替换示意预览）；
- M3：DXF 导出（图层=构件分组）+ 3D3S 导入验证 + SAP2000 .s2k 导出；
- M4：正式截面库（规格/型号联动）+ 荷载组合（NB/T 10115-2018）+ 檩条厂家推荐位置；
- M5：打包免安装 exe；
- 待办：抗震设防数据 OCR 错字校对清单（区县名与规范核对）。

---

## 五、待确认事项

1. 抗震设防数据中个别区县名（OCR 错字）的校对方式：人工清单 or 换文字层规范 PDF；
2. ~~斜梁长度规则（端距+间距 vs 组件总宽）是否以本版为准，M2 时最终统一~~；
   **已确认（2026-08-12）**：现阶段按理论值+取整到10mm即可，误差十几~几十毫米
   工程可接受（下料本就放余量）；梁中心偏移按 100~150mm 经验值（默认150），
   待 M4 檩条/斜梁截面确定后精调；当前优先走通线模数据链路；
3. DXF 沟通机制：用户在 CAD 画支架立面 + 参数标注 → 程序解析生成示意图（待首张 DXF 实测）。
