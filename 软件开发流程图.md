# 光伏支架线模生成器 — 软件开发流程图

> Obsidian 中可直接渲染本 Mermaid 图（代码块语言 `mermaid`）。
> 版本：V0.2.0（2026-08-13，首个正式版）

```mermaid
flowchart TD
    A["需求分析<br/>光伏支架参数化建模 + 多软件线模导出"] --> B["MVP 界面骨架<br/>①组件 ②支架 ③截面 ④3D预览 ⑤荷载"]
    B --> C["参数化数据层<br/>组件/结构/截面/荷载 统一字典"]
    C --> D["几何引擎<br/>按榀数生成节点/杆件/尺寸"]
    D --> E["2D 支架示意 + 3D 线框预览"]
    E --> F["三路导出"]

    F --> F1["DXF 线模<br/>图层分组 + 截面信息"]
    F --> F2["3D3S 文本/Excel<br/>方位角 + K节点 + 截面库"]
    F --> F3["SAP2000 .s2k<br/>材料/截面/荷载/组合/分组"]

    F1 --> G["版本迭代校准<br/>导入实测闭环"]
    F2 --> G
    F3 --> G
    G --> H["V0.2.0 正式版<br/>GitHub 公开 + exe 打包"]
    H --> I["群内测试 → 收集反馈 → 修复维护"]
    I --> J{"反馈收敛？"}
    J -- 否 --> I
    J -- 是 --> K["远期功能规划"]

    K --> K1["BOM 统计 + 报价清单<br/>用钢量 / 每瓦用钢量 / 报价表"]
    K --> K2["风荷载体型系数按规范取值<br/>GB50009-2012 + NB/T 10115-2018"]
    K --> K3["Word 计算书自动生成<br/>python-docx / docxtpl 模板"]
    K --> K4["SAP2000 截面迭代<br/>.OUT 半自动 → OAPI 全自动"]
    K --> K5["支座释放 / 端部约束<br/>界面化设置并写入导出"]

    style H fill:#2e7d32,color:#fff
    style I fill:#1565c0,color:#fff
    style K fill:#6a1b9a,color:#fff
```

## 说明

- **当前阶段**：V0.2.0 已发布，进入 `群内测试 → 反馈 → 修复维护` 循环；
- **开发原则**：先维护稳定、不急于加新功能；反馈收敛后再按 P1~P4 优先级推进；
- **远期功能**：BOM/报价（P1）→ 体型系数自动取值（P2）→ Word 计算书（P3）→
  SAP2000 截面迭代与支座释放（P4）。

配套任务清单详见《需求文档.md》第七章"后续开发规划（任务书）"。
